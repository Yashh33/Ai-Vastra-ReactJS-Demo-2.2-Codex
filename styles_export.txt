:root {
  --bg: #f4f0e8;
  --paper: #fffdfa;
  --ink: #1f2937;
  --ink-soft: #4b5563;
  --line: #ded7cc;
  --accent-dark: #111827;
  --accent-warm: #f4e2bc;
  --accent-cool: #dcedfb;
  --accent-rose: #f4e6d6;
}

* {
  box-sizing: border-box;
}

html,
body,
#root {
  margin: 0;
  min-height: 100%;
}

body {
  font-family: "Manrope", "Avenir Next", "Segoe UI", sans-serif;
  color: var(--ink);
  background:
    radial-gradient(900px 500px at 10% -10%, #fbefe2 0%, transparent 60%),
    radial-gradient(800px 460px at 100% 0%, #e9f3ff 0%, transparent 56%),
    var(--bg);
}

h1,
h2,
p {
  margin: 0;
}

h1 {
  font-size: clamp(1.4rem, 3.6vw, 1.9rem);
  letter-spacing: -0.02em;
}

h2 {
  font-size: 1rem;
  letter-spacing: -0.01em;
}

code {
  background: #f3efe7;
  border-radius: 6px;
  padding: 0.15rem 0.4rem;
}

.screen {
  min-height: 100svh;
  padding: 0.85rem;
}

.screen-centered {
  display: grid;
  place-items: center;
}

.auth-screen {
  display: grid;
  place-items: center;
}

.auth-card,
.page-shell {
  width: min(100%, 960px);
}

.auth-card {
  background: var(--paper);
  border: 1px solid var(--line);
  border-radius: 18px;
  padding: 1rem;
  display: grid;
  gap: 0.8rem;
}

.auth-mode-row {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.4rem;
  padding: 0.25rem;
  border-radius: 12px;
  border: 1px solid #d7d1c6;
  background: #f4ede3;
}

.auth-mode-btn {
  min-height: 40px;
  border: 1px solid transparent;
  border-radius: 10px;
  background: transparent;
  color: #4b5563;
  font-size: 0.9rem;
  font-weight: 700;
  cursor: pointer;
}

.auth-mode-btn-active {
  border-color: #cfd8e8;
  background: #111827;
  color: #fff;
}

.notice-text {
  font-size: 0.84rem;
  color: #166534;
}

.page-shell {
  display: grid;
  gap: 0.7rem;
}

.hero-panel {
  background: linear-gradient(155deg, #182535, #0f172a);
  color: #f8fafc;
  border: 1px solid #1e293b;
  border-radius: 18px;
  padding: 1rem;
  display: grid;
  gap: 0.2rem;
}

.page-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.7rem;
}

.card {
  background: var(--paper);
  border: 1px solid var(--line);
  border-radius: 16px;
  padding: 0.8rem;
}

.stack-sm {
  display: grid;
  gap: 0.6rem;
}

.stack-md {
  display: grid;
  gap: 0.8rem;
}

.row {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 0.55rem;
}

.between {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.55rem;
}

.flex-1 {
  flex: 1;
}

.field {
  display: grid;
  gap: 0.35rem;
  font-size: 0.88rem;
  font-weight: 700;
  color: var(--ink-soft);
}

.field input,
.field textarea,
.field select,
.range-field input[type="range"] {
  font: inherit;
}

.field input,
.field textarea,
.field select {
  width: 100%;
  min-height: 44px;
  border: 1px solid #d8d1c6;
  border-radius: 12px;
  background: #fff;
  padding: 0.65rem 0.75rem;
  color: var(--ink);
}

.field textarea {
  min-height: 92px;
  resize: vertical;
}

.field select {
  min-height: 44px;
  background:
    linear-gradient(45deg, transparent 50%, #6b7280 50%) calc(100% - 16px) calc(50% - 3px) / 7px 7px no-repeat,
    linear-gradient(135deg, #6b7280 50%, transparent 50%) calc(100% - 11px) calc(50% - 3px) / 7px 7px no-repeat,
    #fff;
  padding-right: 2rem;
  appearance: none;
}

.btn {
  min-height: 44px;
  border-radius: 12px;
  border: 1px solid transparent;
  padding: 0.58rem 0.88rem;
  font-size: 0.93rem;
  font-weight: 800;
  cursor: pointer;
  transition: transform 120ms ease, background 120ms ease, border-color 120ms ease, opacity 120ms ease;
}

.btn:active {
  transform: translateY(1px);
}

.btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.btn-dark {
  background: var(--accent-dark);
  color: #fff;
  border-color: #0b1220;
}

.btn-light {
  background: #fff;
  color: var(--ink);
  border-color: #d8d1c6;
}

.home-card {
  position: relative;
  min-height: 124px;
  border: 1px solid #d7d1c6;
  border-radius: 18px;
  padding: 1rem;
  text-align: left;
  display: grid;
  gap: 0.35rem;
  cursor: pointer;
}

.home-card strong {
  font-size: 1.3rem;
}

.home-card span {
  color: #374151;
  font-size: 0.83rem;
}

.home-card-badge {
  position: absolute;
  top: 0.8rem;
  right: 0.8rem;
  width: 28px;
  height: 28px;
  border-radius: 999px;
  background: #0f172a;
  color: #fff;
  display: grid;
  place-items: center;
  font-size: 0.72rem;
  font-weight: 800;
}

.home-card-visualize {
  background: var(--accent-warm);
}

.home-card-hero {
  background: var(--accent-cool);
}

.home-card-history {
  background: var(--accent-rose);
}

.mode-card {
  min-height: 124px;
  border: 1px solid #d7d1c6;
  border-radius: 18px;
  padding: 1rem;
  text-align: left;
  display: grid;
  gap: 0.35rem;
  cursor: pointer;
}

.mode-card strong {
  font-size: 1.3rem;
}

.mode-card span {
  color: #374151;
  font-size: 0.83rem;
}

.mode-card-create {
  background: #e9f3ff;
}

.mode-card-catalog {
  background: #f8ecd8;
}

.switch-row {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  min-height: 44px;
  font-size: 0.82rem;
  color: var(--ink-soft);
}

.switch-row input {
  width: 18px;
  height: 18px;
}

.loading-box,
.empty-box {
  min-height: 132px;
  border-radius: 12px;
  border: 1px dashed #d8d1c6;
  display: grid;
  place-items: center;
  gap: 0.4rem;
  padding: 0.8rem;
  text-align: center;
  color: #6b7280;
}

.loading-box.dark,
.carousel-viewer .loading-box,
.viewer-card .loading-box {
  border: 1px solid #334155;
  background: #111827;
}

.spinner {
  width: 26px;
  height: 26px;
  border-radius: 999px;
  border: 3px solid #d8dbe2;
  border-top-color: #111827;
  animation: spin 0.75s linear infinite;
}

.dark .spinner,
.loading-box.dark .spinner {
  border-color: #334155;
  border-top-color: #f8fafc;
}

.spinner-small {
  width: 18px;
  height: 18px;
  border-width: 2px;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.tile-grid {
  display: grid;
  gap: 0.68rem;
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.folder-tile,
.image-tile,
.output-tile {
  border: 1px solid #dfd8cc;
  border-radius: 14px;
  background: #fff;
}

.folder-tile {
  min-height: 150px;
  text-align: left;
  display: grid;
  gap: 0.3rem;
  padding: 0.65rem;
  cursor: pointer;
}

.folder-tile strong {
  font-size: 0.96rem;
}

.folder-tile-inactive {
  opacity: 0.65;
}

.folder-icon {
  width: 58px;
  height: 36px;
  border-radius: 10px;
  background:
    linear-gradient(#cfd7e3 0 0) top 6px left 8px/22px 10px no-repeat,
    linear-gradient(#dce3ef 0 0) center/100% 78% no-repeat;
}

.image-tile {
  overflow: hidden;
  text-align: left;
  cursor: pointer;
  display: grid;
}

.image-tile img,
.output-tile img,
.preview-image {
  width: 100%;
  object-fit: cover;
  border-radius: 12px;
}

.image-tile img {
  height: 170px;
}

.preview-image {
  height: min(58vw, 250px);
}

.image-placeholder {
  min-height: 170px;
  border: 1px dashed #d9d1c4;
  background: #f7f5f1;
  border-radius: 12px;
  display: grid;
  place-items: center;
}

.tile-chip {
  justify-self: start;
  margin: 0.35rem 0.35rem 0;
  padding: 0.25rem 0.48rem;
  font-size: 0.67rem;
  font-weight: 800;
  border-radius: 999px;
  background: #e3f4ff;
  color: #0c4a6e;
}

.image-tile .tiny {
  margin: 0.35rem;
}

.preview-placeholder {
  min-height: 130px;
  border-radius: 12px;
  border: 1px dashed #d9d1c4;
  background: #f7f4ef;
  display: grid;
  place-items: center;
  color: #6b7280;
  text-align: center;
  padding: 0.8rem;
}

.fabric-draft-card {
  border: 1px solid #dfd8cc;
  border-radius: 14px;
  padding: 0.65rem;
  background: #fff;
}

.select-input {
  font-weight: 700;
}

.output-tile {
  position: relative;
  padding: 0.45rem;
}

.output-tile-press {
  width: 100%;
  border: 0;
  padding: 0;
  background: transparent;
  cursor: pointer;
}

.output-tile img {
  height: 170px;
}

.download-chip {
  position: absolute;
  top: 0.8rem;
  right: 0.8rem;
  min-width: 32px;
  min-height: 32px;
  border-radius: 999px;
  border: 0;
  background: rgba(15, 23, 42, 0.9);
  color: #fff;
  font-weight: 800;
  cursor: pointer;
}

.delete-chip {
  position: absolute;
  top: 0.8rem;
  right: 3.35rem;
  min-width: 34px;
  min-height: 32px;
  border-radius: 999px;
  border: 0;
  background: rgba(127, 29, 29, 0.92);
  color: #fff;
  font-weight: 800;
  cursor: pointer;
}

.download-chip:disabled,
.delete-chip:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.catalog-screen {
  background: #f4f4f5;
}

.catalog-shell {
  width: min(100%, 980px);
  margin: 0 auto;
  display: grid;
  gap: 0.65rem;
}

.catalog-header {
  position: sticky;
  top: 0;
  z-index: 5;
  background: #f8fafc;
  border: 1px solid #e5e7eb;
  border-radius: 14px;
  padding: 0.55rem 0.65rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.6rem;
}

.catalog-header-left {
  display: flex;
  align-items: center;
  gap: 0.55rem;
  min-width: 0;
}

.catalog-back-btn {
  width: 36px;
  height: 36px;
  border-radius: 999px;
  border: 1px solid #d1d5db;
  background: #fff;
  color: #111827;
  font-size: 1.1rem;
  line-height: 1;
  cursor: pointer;
}

.catalog-brand-mark {
  min-width: 34px;
  height: 34px;
  border-radius: 9px;
  background: linear-gradient(145deg, #f43f5e, #f59e0b);
  color: #fff;
  font-size: 0.69rem;
  font-weight: 800;
  display: grid;
  place-items: center;
}

.catalog-title-wrap {
  min-width: 0;
}

.catalog-title {
  margin: 0;
  font-size: 1.1rem;
  font-weight: 800;
  color: #111827;
}

.catalog-subtitle {
  margin: 0.1rem 0 0;
  font-size: 0.73rem;
  color: #6b7280;
}

.catalog-header-actions {
  display: flex;
  align-items: center;
  gap: 0.45rem;
}

.catalog-icon-btn {
  width: 34px;
  height: 34px;
  display: inline-grid;
  place-items: center;
  border-radius: 999px;
  border: 1px solid #d1d5db;
  background: #fff;
  color: #374151;
  font-size: 0.92rem;
  cursor: pointer;
}

.catalog-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.6rem;
}

.catalog-tile {
  background: #fff;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  overflow: hidden;
  display: grid;
}

.catalog-image-btn {
  border: 0;
  padding: 0;
  margin: 0;
  background: transparent;
  cursor: pointer;
}

.catalog-image {
  width: 100%;
  aspect-ratio: 3 / 4;
  object-fit: cover;
  display: block;
  background: #f3f4f6;
}

.catalog-meta-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.45rem;
  padding: 0.45rem 0.5rem 0.5rem;
}

.catalog-garment-label {
  margin: 0;
  font-size: 0.83rem;
  color: #111827;
  font-weight: 700;
  line-height: 1.25;
  flex: 1;
  min-width: 0;
}

.catalog-action-row {
  display: flex;
  align-items: center;
  gap: 0.4rem;
}

.catalog-chip-btn {
  min-width: 38px;
  min-height: 30px;
  display: inline-grid;
  place-items: center;
  line-height: 1;
  border: 0;
  border-radius: 999px;
  padding: 0 0.55rem;
  color: #fff;
  font-weight: 800;
  font-size: 0.75rem;
  cursor: pointer;
}

.catalog-chip-delete {
  background: #991b1b;
}

.catalog-chip-download {
  background: #1e293b;
}

.catalog-chip-btn:disabled,
.catalog-icon-btn:disabled,
.catalog-back-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.viewer-screen {
  background: linear-gradient(180deg, #f0ece4 0%, #ece5d9 100%);
}

.viewer-shell {
  gap: 0.65rem;
}

.viewer-card {
  min-height: 58svh;
  border-radius: 16px;
  border: 1px solid #334155;
  background: #111827;
  display: grid;
  overflow: hidden;
}

.viewer-card img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  background: #111827;
}

.muted {
  color: #6b7280;
}

.muted-light {
  color: #cbd5e1;
}

.tiny {
  font-size: 0.8rem;
}

.error-text {
  font-size: 0.84rem;
  color: #b91c1c;
}

.match-preview-wrap {
  position: relative;
  overflow: hidden;
  border-radius: 14px;
  border: 1px solid #334155;
  background: #111827;
  padding: 0.35rem;
  display: grid;
  place-items: center;
}

.match-preview-canvas {
  width: 100%;
  height: auto;
  max-height: min(70vh, 540px);
  border-radius: 10px;
  cursor: crosshair;
}

.inspect-focus-dot {
  position: absolute;
  width: 18px;
  height: 18px;
  border-radius: 999px;
  border: 2px solid rgb(248 250 252 / 95%);
  box-shadow: 0 0 0 2px rgb(15 23 42 / 70%);
  transform: translate(-50%, -50%);
  pointer-events: none;
}

.inspect-help-text {
  margin-top: 0.1rem;
}

.inspect-canvas-wrap {
  border-radius: 12px;
  border: 1px solid #334155;
  background: #0f172a;
  padding: 0.35rem;
  display: grid;
  place-items: center;
  width: fit-content;
  max-width: 100%;
  margin-inline: auto;
}

.inspect-canvas {
  width: 240px;
  height: 240px;
  border-radius: 10px;
  background: #111827;
  display: block;
}

.swatch-row {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.swatch-btn {
  width: 44px;
  height: 44px;
  border-radius: 999px;
  border: 2px solid #d2d1d1;
  background: #fff;
  display: grid;
  place-items: center;
  padding: 0;
  cursor: pointer;
}

.swatch-btn span {
  width: 28px;
  height: 28px;
  border-radius: 999px;
  border: 1px solid #d1d5db;
}

.swatch-selected {
  border-color: #0f172a;
  border-width: 3px;
}

.swatch-edited {
  box-shadow: inset 0 0 0 2px #0f766e;
}

.range-field {
  display: grid;
  gap: 0.32rem;
  font-size: 0.88rem;
  color: #374151;
}

.range-field input[type="range"] {
  width: 100%;
}

.range-input {
  width: 100%;
  height: 24px;
  margin: 0;
  background: transparent;
  -webkit-appearance: none;
  appearance: none;
}

.range-input:focus {
  outline: none;
}

.range-input::-webkit-slider-runnable-track {
  height: 7px;
  border-radius: 999px;
  background: var(--track-gradient, #7b7b7b);
}

.range-input::-webkit-slider-thumb {
  width: 18px;
  height: 18px;
  margin-top: -5.5px;
  border-radius: 50%;
  border: 2px solid #4b5563;
  background: #f8fafc;
  box-shadow: 0 1px 3px rgb(0 0 0 / 0.25);
  -webkit-appearance: none;
  appearance: none;
}

.range-input::-moz-range-track {
  height: 7px;
  border-radius: 999px;
  background: var(--track-gradient, #7b7b7b);
}

.range-input::-moz-range-progress {
  background: transparent;
}

.range-input::-moz-range-thumb {
  width: 18px;
  height: 18px;
  border-radius: 50%;
  border: 2px solid #4b5563;
  background: #f8fafc;
  box-shadow: 0 1px 3px rgb(0 0 0 / 0.25);
}

.range-input:disabled::-webkit-slider-thumb,
.range-input:disabled::-moz-range-thumb {
  opacity: 0.55;
}

.chip {
  font-size: 0.74rem;
  padding: 0.25rem 0.6rem;
  border-radius: 999px;
  border: 1px solid #cbd5e1;
  background: #f1f5f9;
}

.carousel-screen {
  background: radial-gradient(circle at 20% 0%, #1e293b 0%, #020617 50%, #01040f 100%);
}

.carousel-shell h1 {
  color: #f8fafc;
}

.carousel-viewer {
  min-height: 60svh;
  border-radius: 18px;
  border: 1px solid #334155;
  background: #111827;
  overflow: hidden;
  cursor: pointer;
}

.carousel-viewer img {
  width: 100%;
  height: 100%;
  object-fit: contain;
}

.counter-chip {
  min-height: 44px;
  border-radius: 999px;
  border: 1px solid #334155;
  background: #111827;
  color: #e2e8f0;
  display: inline-grid;
  place-items: center;
  padding: 0 0.75rem;
  font-size: 0.82rem;
  font-weight: 700;
}

@media (min-width: 880px) {
  .screen {
    padding: 1.1rem;
  }

  .page-shell {
    gap: 0.95rem;
  }

  .tile-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .output-tile img,
  .image-tile img {
    height: 210px;
  }

  .catalog-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}



/* Hero image tile actions */
.image-tile {
  display: grid;
  gap: 0.35rem;
  padding: 0.4rem;
}

.image-tile-press {
  width: 100%;
  border: 0;
  padding: 0;
  margin: 0;
  background: transparent;
  border-radius: 12px;
  overflow: hidden;
  cursor: pointer;
}

.image-tile-press:disabled {
  cursor: not-allowed;
  opacity: 0.8;
}

.image-tile .tiny {
  margin: 0;
}

.image-tile-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.45rem;
}

.tile-meta-stack {
  display: grid;
  gap: 0.25rem;
}

.tile-delete-btn {
  min-height: 34px;
  border-radius: 999px;
  border: 0;
  padding: 0.3rem 0.7rem;
  background: #991b1b;
  color: #fff;
  font-size: 0.75rem;
  font-weight: 800;
  cursor: pointer;
  white-space: nowrap;
}

.tile-delete-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

/* Global app chrome */
.app-shell {
  min-height: 100svh;
  display: grid;
  grid-template-rows: auto 1fr auto;
  background:
    radial-gradient(880px 460px at 0% -10%, #f9ecdc 0%, transparent 62%),
    radial-gradient(840px 420px at 100% -10%, #e8f2ff 0%, transparent 56%),
    var(--bg);
}

.app-shell-header {
  position: sticky;
  top: 0;
  z-index: 30;
  min-height: 68px;
  padding: 0.55rem 0.8rem;
  border-bottom: 1px solid #d8d1c6;
  background: rgba(255, 252, 246, 0.94);
  backdrop-filter: blur(6px);
  display: flex;
  align-items: center;
  gap: 0.7rem;
}

.app-shell-content {
  min-height: 0;
  overflow: auto;
}

.app-shell-content .screen {
  min-height: auto;
}

.app-shell-footer {
  min-height: 46px;
  border-top: 1px solid #d8d1c6;
  background: #fff9f0;
  color: #334155;
  font-size: 0.86rem;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  letter-spacing: 0.01em;
}

.app-icon-btn {
  width: 40px;
  height: 40px;
  border-radius: 999px;
  border: 1px solid #d2c9ba;
  background: #fff;
  color: #111827;
  font-size: 1rem;
  cursor: pointer;
  display: inline-grid;
  place-items: center;
}

.app-icon-btn:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}

 .app-brand {
  flex: 1;
  min-width: 0;
  display: flex;
  align-items: center;
}

.app-brand-logo {
  width: min(150px, 44vw);
  max-height: 36px;
  height: auto;
  border-radius: 8px;
  border: 1px solid #d4ccbf;
  background: #fff;
  object-fit: contain;
}

.app-brand-fallback {
  width: 40px;
  height: 40px;
  border-radius: 10px;
  border: 1px solid #d4ccbf;
  background: #fff;
  object-fit: contain;
  flex-shrink: 0;
}

.app-brand-fallback {
  display: grid;
  place-items: center;
  font-size: 0.72rem;
  font-weight: 800;
  color: #0f172a;
  background: linear-gradient(145deg, #f43f5e, #f59e0b);
  border-color: transparent;
  color: #fff;
}

.app-brand-copy {
  min-width: 0;
  display: grid;
  gap: 0.08rem;
}

.app-brand-copy strong {
  font-size: 0.92rem;
  color: #0f172a;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.app-brand-copy span {
  font-size: 0.74rem;
  color: #64748b;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}


/* App header menu */
.app-menu-wrap {
  position: relative;
  flex-shrink: 0;
}

.app-menu-btn {
  width: 40px;
  height: 40px;
  border-radius: 10px;
  border: 1px solid #d2c9ba;
  background: #fff;
  color: #111827;
  font-size: 1.2rem;
  line-height: 1;
  display: inline-grid;
  place-items: center;
  cursor: pointer;
}

.app-menu-popover {
  position: absolute;
  top: calc(100% + 0.5rem);
  right: 0;
  min-width: 190px;
  border-radius: 12px;
  border: 1px solid #d9d1c4;
  background: #fff;
  box-shadow: 0 10px 24px rgb(15 23 42 / 18%);
  padding: 0.35rem;
  display: grid;
  gap: 0.22rem;
  z-index: 40;
}

.app-menu-item {
  width: 100%;
  min-height: 40px;
  border: 1px solid transparent;
  border-radius: 10px;
  background: #fff;
  color: #111827;
  text-align: left;
  font-size: 0.9rem;
  font-weight: 700;
  padding: 0.45rem 0.65rem;
  cursor: pointer;
}

.app-menu-item:hover {
  background: #f8fafc;
  border-color: #e2e8f0;
}

.app-menu-item-danger {
  color: #991b1b;
}

.app-menu-item:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.catalog-icon-btn-active {
  background: #0f172a;
  border-color: #0b1220;
  color: #fff;
}

/* Equal-fill card layouts for mode + create-looks screens */
.screen-fill {
  height: 100%;
  min-height: 100%;
  display: flex;
}

.screen-fill .page-shell {
  flex: 1;
}

.equal-fill-shell {
  min-height: 100%;
  display: grid;
  gap: 0.75rem;
}

.equal-fill-shell-2 {
  grid-template-rows: repeat(2, minmax(0, 1fr));
}

.equal-fill-shell-3 {
  grid-template-rows: repeat(3, minmax(0, 1fr));
}

.equal-fill-card {
  height: 100%;
  min-height: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  gap: 0.55rem;
  padding: 0.9rem;
}

/* .equal-fill-card strong {
  font-size: clamp(1.35rem, 5.2vw, 1.95rem);
  line-height: 1.1;
} */

.equal-fill-card strong {
  font-size: clamp(3.3rem, 7vw, 2.6rem);
  line-height: 1.1;
  color: rgba(11, 15, 26, 0.85);
}

.equal-fill-shell-3 .equal-fill-card strong {
  font-size: clamp(1.8rem, 6vw, 2.3rem);
}


.equal-fill-card span {
  margin: 0;
  font-size: 0.96rem;
  color: #334155;
  line-height: 1.35;
  max-width: 28ch;
}

.equal-fill-card-title-only {
  gap: 0;
}

